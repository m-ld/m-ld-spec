const Clone = require('./clone');

/**
 * Non-chaotic clone death & recovery tests
 */
describe('Killing', () => {
  let clone;

  it('Can restart after', () => {

  });

  // afterEach(async () => await clone.destroy());
});

